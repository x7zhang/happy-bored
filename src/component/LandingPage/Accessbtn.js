//display 2 access buttons;
//one sign in, one log out;
//click button, navigate to related page;
import React from 'react';

const AccessBtn = () => {
    return (
        <div>
            <button>Sign In</button> | <button>Log out</button>
        </div>
    );
};

export default AccessBtn;